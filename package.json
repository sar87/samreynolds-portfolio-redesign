{
  "name": "sam-portfolio-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/build-notes.js && vite",
    "build": "node scripts/build-notes.js && tsc && vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-content.js",
    "notes": "node scripts/build-notes.js"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^5.2.1",
    "marked": "^18.0.5",
    "typescript": "~5.9.3",
    "vite": "^5.4.21"
  }
}
